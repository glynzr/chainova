{
  "name": "@chainova/blockchain",
  "private": true,
  "scripts": {
    "dev": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat run --network localhost scripts/deploy.js",
    "simulate": "hardhat run --network localhost scripts/simulate.js"
  },
  "dependencies": {
    "hardhat": "^2.22.13",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5"
  }
}
